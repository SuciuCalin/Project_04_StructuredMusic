<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.android.structuredmusic">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:name=".MainActivity"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".AlbumsActivity"
            android:label="@string/albumsCategory"
            android:screenOrientation="portrait" />
        <activity
            android:name=".SongsActivity"
            android:label="@string/songsCategory"
            android:screenOrientation="portrait" />
        <activity
            android:name=".PlaylistsActivity"
            android:label="@string/playlistsCategory"
            android:screenOrientation="portrait" />
        <activity
            android:name=".NowPlayingActivity"
            android:label="@string/nowPlayingCategory"
            android:screenOrientation="portrait" />

    </application>
</manifest>